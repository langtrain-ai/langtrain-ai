# Tutorials

## Fine-tuning a model

1. Prepare your data in CSV format.
2. Run `langtrain prepare`.
3. Run `langtrain tune --config config.json`.

## Vision to Text

1. Use `langvision` to process images.
2. Generate a manifest.
3. Feed into `langtune`.
