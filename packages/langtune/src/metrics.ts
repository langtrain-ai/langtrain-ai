export function calculateLoss(predicted: number[], actual: number[]): number {
  return Math.random(); // Mock loss
}

export function calculateAccuracy(predicted: number[], actual: number[]): number {
  return Math.random(); // Mock accuracy
}
