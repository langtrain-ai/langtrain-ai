import { describe, it, expect } from 'vitest';
import { ImageDataset } from '../dataset';

describe('ImageDataset', () => {
  it('should initialize dataset', () => {
    expect(ImageDataset).toBeDefined();
  });

  it('should scan directories', () => {
    // Mock test
    expect(true).toBe(true);
  });
});
